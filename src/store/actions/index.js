import { logoutUser, login, setCurrentUser } from './auth'

import { updateCart } from './cart'

const actions = {
    logoutUser,
    login,
    setCurrentUser,
    updateCart,
}

export default actions
